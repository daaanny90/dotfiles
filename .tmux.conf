unbind C-b
set -g prefix C-a

# Split View
unbind '"'
unbind %
bind | split-window -h
bind - split-window -v

# Set correct support for italic fonts
set -g default-terminal "tmux-256color"

# set-option -a terminal-overrides ",iterm-256color:RGB"
# set-option -a terminal-overrides ",*256col*:RGB"
# set-option -ga terminal-overrides ",xterm-256color:Tc"
# reload configuration
unbind r
bind r source-file ~/.tmux.conf \; display-message "Config reloaded"

# undercurl support
# set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
# support colors for undercurl
# set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# switch panes using jkhl
bind h select-pane -L
bind l select-pane -R
bind j select-pane -U
bind k select-pane -D

# maximize current panel
bind -r m resize-pane -Z

# Mouse
set -g mouse on

# Remove delay when hitting escape
set -s escape-time 0

# Refresh status line every 5 seconds - Good for when music is playing / update time etc
set -g status-interval 5

# start numbering from 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# 1c1e20

###### DESIGN TWEAKS ######

# Refresh status line every 5 seconds - Good for when music is playing / update time etc
set -g status-interval 5

# Start window and pane indices at 1.
set -g base-index 1
set -g pane-base-index 0

# Length of tmux status line
set -g status-left-length 30
set -g status-right-length 150

set-option -g status "on"


# # Top statusbar
set-option -g status-position top

# SOLARIZED OSAKA THEME
set -g mode-style "fg=#7aa2f7,bg=#3b4261"

set -g message-style "fg=#7aa2f7,bg=#3b4261"
set -g message-command-style "fg=#7aa2f7,bg=#3b4261"

set -g pane-border-style "fg=#3b4261"
set -g pane-active-border-style "fg=#7aa2f7"

set -g status "on"
set -g status-justify "left"

set -g status-style "fg=#7aa2f7,bg=#1a1919"

set -g status-left-length "100"
set -g status-right-length "100"

set -g status-left-style NONE
set -g status-right-style NONE

set -g status-left "#[fg=#1a1919,bg=#7aa2f7,bold] #S #[fg=#7aa2f7,bg=#1a1919,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=#1a1919,bg=#1a1919,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#1a1919] #{prefix_highlight} #[fg=#3b4261,bg=#1a1919,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#3b4261] ♥ #(~/.config/tmux/scripts/battery.sh) #[fg=#7aa2f7,bg=#3b4261] %Y-%m-%d  %I:%M %p #[fg=#7aa2f7,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#1a1919,bg=#7aa2f7,bold] #h "
if-shell '[ "$(tmux show-option -gqv "clock-mode-style")" == "24" ]' {
  set -g status-right "#[fg=#1a1919,bg=#1a1919,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#1a1919] #{prefix_highlight} #[fg=#3b4261,bg=#1a1919,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#3b4261] ♥ #(~/.config/tmux/scripts/battery.sh)  #[fg=#7aa2f7,bg=#3b4261] %Y-%m-%d  %H:%M #[fg=#7aa2f7,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#1a1919,bg=#7aa2f7,bold] #h "
}

setw -g window-status-activity-style "underscore,fg=#a9b1d6,bg=#1a1919"
setw -g window-status-separator ""
setw -g window-status-style "NONE,fg=#a9b1d6,bg=#1a1919"
setw -g window-status-format "#[fg=#1a1919,bg=#1a1919,nobold,nounderscore,noitalics]#[default] #I  #W #F #[fg=#1a1919,bg=#1a1919,nobold,nounderscore,noitalics]"
setw -g window-status-current-format "#[fg=#1a1919,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#3b4261,bold] #I  #W #F #[fg=#3b4261,bg=#1a1919,nobold,nounderscore,noitalics]"

# tmux-plugins/tmux-prefix-highlight support
set -g @prefix_highlight_output_prefix "#[fg=#e0af68]#[bg=#1a1919]#[fg=#1a1919]#[bg=#e0af68]"
set -g @prefix_highlight_output_suffix ""


# PLUGINS

## VIM TMUX NAVIGATOR
# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l
