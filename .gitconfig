[credential]
	helper = osxkeychain
[user]
	name = Danny
	email = hi@dannyspina.com
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
